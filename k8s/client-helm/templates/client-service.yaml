apiVersion: v1
kind: Service
metadata:
  name: ecom-client
spec:
  type: {{ .Values.client.service.type }}
  selector:
    app: ecom-client
  ports:
    - port: {{ .Values.client.service.port }}
      targetPort: {{ .Values.client.service.port }}